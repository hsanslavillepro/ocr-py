apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ocr-api-prod

resources:
  - ../../base
  - namespace.yaml

images:
  - name: ocr-api
    newName: ocr-api
    newTag: prod

patches:
  - path: configmap-patch.yaml
  - path: deployment-patch.yaml
  - path: hpa-patch.yaml
  - path: pvc-patch.yaml
